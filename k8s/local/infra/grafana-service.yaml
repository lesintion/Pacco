apiVersion: v1
kind: Service
metadata:
  annotations:

    kompose.version: 1.19.0

  labels:
    infra.service: grafana
  name: grafana
spec:
  ports:
  - name: "3000"
    port: 3000
    targetPort: 3000
  selector:
    infra.service: grafana
status:
  loadBalancer: {}
